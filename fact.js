function fact(){
        var now = new Date()
        if (0==now.getHours() && 0==now.getMinutes()){
                document.write(quux);
        }
        setTimeout(timer_passage, 1000)//exec myself after 60000 ms
}
